/* Chương trình in ra 2 dòng chữ. Bản 1 (010401B1.C) */
#include <stdio.h> /* Sự dụng thư viện vào ra chuẩn */
#include <conio.h>
#include <stdlib.h>

int main() /* Hàm chính */
{
   system("cls"); /* Xóa màn hinh */
   printf("\nTURBO C HAN HANH");
   /* Xuống dòng và in: LAM QUEN VOI BAN */
   printf("\nLAM QUEN VOI BAN");
   getch(); /* Tạm dừng máy để xem kết quả */

   return 0;
}