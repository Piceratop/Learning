#include <stdio.h>

int main()
{
    int i, j;
    float x, y;
    // scanf("%d%d%f%f", &i, &j, &x, &y);
    // printf("%d %d %f %f\n", i, j, x, y);
    // scanf("%d%d%f%f", &i, &j, &x, &y);
    // printf("%d %d %f %f\n", i, j, x, y);
    scanf("%d/%d", &i, &j);
    printf("%d %d\n", i, j);
    return 0;
}