def print_line(text):
    """
    A function to print a line of dashes above and below the given text.

    Parameters:
    text (str): The text to be printed

    Returns:
    None
    """
    print("-" * len(text))
    print(text)
    print("-" * len(text))
